name: Enhanced CI/CD Pipeline

on:
  schedule:
    - cron: '0 3 * * 1'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  RUST_LOG: info
  CARGO_INCREMENTAL: 0
  TEST_FEATURES: "rustls-tls,compression,http2,connection-pool,dns"

jobs:
  # ============================================================
  # Lint and Format Checks
  # ============================================================
  lint:
    name: Lint and Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      
      - name: Cache cargo
        uses: actions/cache@v3
        with:
          path: target
          key: ${{ runner.os }}-cargo-lint-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-lint-
      
      - name: Check formatting
        run: cargo fmt --all -- --check
      
      - name: Run clippy on default members
        run: cargo clippy --all-targets --features "${{ env.TEST_FEATURES }}" -- -D warnings
      
      - name: Run clippy on default-member tests
        run: cargo clippy --tests --features "${{ env.TEST_FEATURES }}" -- -D warnings

  # ============================================================
  # Security Audit
  # ============================================================
  security:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Cache cargo
        uses: actions/cache@v3
        with:
          path: target
          key: ${{ runner.os }}-cargo-audit-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-audit-
      
      - name: Install cargo-audit
        run: cargo install cargo-audit --locked
      
      - name: Run security audit
        run: cargo audit
      
      - name: Run cargo-deny
        uses: EmbarkStudios/cargo-deny-action@v2
        with:
          log-level: warn
          command: check

  # ============================================================
  # Compilation Checks
  # ============================================================
  compile:
    name: Compile (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Cache cargo
        uses: actions/cache@v3
        with:
          path: target
          key: ${{ runner.os }}-cargo-compile-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-compile-
      
      - name: Check default-member compilation (debug)
        run: cargo check --features "${{ env.TEST_FEATURES }}" --verbose
      
      - name: Check default-member compilation (release)
        run: cargo build --release --features "${{ env.TEST_FEATURES }}" --verbose

  # ============================================================
  # Unit Tests
  # ============================================================
  test:
    name: Unit Tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Install nextest
        uses: taiki-e/install-action@nextest
      
      - name: Cache cargo
        uses: actions/cache@v3
        with:
          path: target
          key: ${{ runner.os }}-cargo-test-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-test-
      
      - name: Run default-member unit tests
        run: cargo nextest run --lib --features "${{ env.TEST_FEATURES }}"
      
      - name: Run default-member doc tests
        run: cargo test --doc --features "${{ env.TEST_FEATURES }}"

  # ============================================================
  # Integration Tests
  # ============================================================
  integration-tests:
    name: Integration Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Install nextest
        uses: taiki-e/install-action@nextest
      
      - name: Cache cargo
        uses: actions/cache@v3
        with:
          path: target
          key: ${{ runner.os }}-cargo-integration-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-integration-
      
      - name: Run default-member integration tests
        run: cargo nextest run --test '*' --features "${{ env.TEST_FEATURES }}"

  # ============================================================
  # Code Coverage
  # ============================================================
  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Install tarpaulin
        run: cargo install cargo-tarpaulin
      
      - name: Cache cargo
        uses: actions/cache@v3
        with:
          path: target
          key: ${{ runner.os }}-cargo-coverage-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-coverage-
      
      - name: Generate coverage for default members
        run: cargo tarpaulin --features "${{ env.TEST_FEATURES }}" --timeout 300 --out xml --output-dir ./coverage
      
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage/cobertura.xml
          flags: rust
          fail_ci_if_error: false

  # ============================================================
  # Benchmarks
  # ============================================================
  benchmarks:
    name: Performance Benchmarks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Cache cargo
        uses: actions/cache@v3
        with:
          path: target
          key: ${{ runner.os }}-cargo-bench-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-bench-
      
      - name: Run benchmarks
        id: run-benchmarks
        run: |
          # Note: Project currently has no defined benchmarks
          # Only compile benchmarks to verify they build correctly
          cargo bench --features "${{ env.TEST_FEATURES }}" --no-run 2>&1 | tee benchmark-results.txt
          echo "Benchmark compilation completed successfully"
      
      - name: Store benchmark result
        # Only run if benchmark output was generated
        continue-on-error: true
        uses: benchmark-action/github-action-benchmark@v1
        with:
          tool: 'cargo'
          output-file-path: benchmark-results.txt
          github-token: ${{ secrets.GITHUB_TOKEN }}
          auto-push: true

  # ============================================================
  # Documentation
  # ============================================================
  docs:
    name: Documentation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Cache cargo
        uses: actions/cache@v3
        with:
          path: target
          key: ${{ runner.os }}-cargo-docs-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-docs-
      
      - name: Build default-member documentation
        run: cargo doc --features "${{ env.TEST_FEATURES }}" --no-deps
      
      - name: Check documentation links
        run: |
          cd target/doc
          find . -name "*.html" -type f | head -20

  # ============================================================
  # Feature Matrix Testing
  # ============================================================
  feature-test:
    name: Feature Matrix (${{ matrix.features }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        features:
          - ""
          - "rustls-tls"
          - "compression"
          - "http2"
          - "connection-pool"
          - "dns"
          - "api-noise"
          - "defense"
          - "rustls-tls,compression,http2,connection-pool,dns,defense"
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Cache cargo
        uses: actions/cache@v3
        with:
          path: target
          key: ${{ runner.os }}-cargo-features-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-features-
      
      - name: Test default members with features
        run: cargo test --features "${{ matrix.features }}"

  # ============================================================
  # MIRI (Undefined Behavior Detection)
  # ============================================================
  miri:
    name: MIRI Undefined Behavior Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: miri
      
      - name: Cache cargo
        uses: actions/cache@v3
        with:
          path: target
          key: ${{ runner.os }}-cargo-miri-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-miri-
      
      - name: Run tests under MIRI
        run: cargo +nightly miri test --workspace --lib
        env:
          MIRIFLAGS: -Zmiri-strict-provenance -Zmiri-disable-isolation

  # ============================================================
  # Status Check
  # ============================================================
  all-checks:
    name: All Checks Pass
    needs: [lint, security, compile, test, integration-tests, coverage, benchmarks, docs]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Check status
        run: |
          if [ "${{ needs.lint.result }}" == "failure" ] || \
             [ "${{ needs.security.result }}" == "failure" ] || \
             [ "${{ needs.compile.result }}" == "failure" ] || \
             [ "${{ needs.test.result }}" == "failure" ] || \
             [ "${{ needs.integration-tests.result }}" == "failure" ]; then
            echo "❌ One or more checks failed"
            exit 1
          fi
          echo "✅ All checks passed"
