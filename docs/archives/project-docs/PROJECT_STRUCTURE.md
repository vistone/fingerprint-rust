# 项目结构说明

**版本**: v2.1.0  
**最后更新**: 2026-02-13

## 📁 目录结构概览

```
fingerprint-rust/
├── config/                    # 所有配置文件
│   ├── build/                # 构建配置 (Cargo.toml, rust-toolchain.toml)
│   ├── deployment/           # 部署配置
│   │   ├── k8s/             # Kubernetes配置
│   │   ├── scripts/         # 部署脚本
│   │   └── docker/          # Docker配置
│   ├── monitoring/           # 监控配置
│   │   ├── prometheus/      # Prometheus配置
│   │   ├── grafana/         # Grafana仪表板
│   │   └── alertmanager/    # 告警配置
│   └── services/             # 服务配置
│       ├── ml-api/          # ML服务配置
│       └── gateway/         # 网关配置
│
├── crates/                   # Rust工作区 (20个crate)
│   ├── fingerprint-core/     # 核心类型和工具函数
│   ├── fingerprint-tls/      # TLS配置和握手
│   ├── fingerprint-profiles/ # 浏览器指纹配置
│   ├── fingerprint-http/     # HTTP客户端实现
│   ├── fingerprint-gateway/  # API网关 (Rust) ✅ 推荐
│   └── ...                   # 其他功能模块
│
├── data/                     # 数据文件
│   └── config/              # 配置数据文件
│       └── dnsservernames.json  # DNS服务器名称列表
│
├── dataset/                  # 机器学习数据集
│   ├── metadata.json        # 数据集元数据
│   ├── train_set.csv        # 训练集
│   ├── val_set.csv          # 验证集
│   └── test_set.csv         # 测试集
│
├── docs/                     # 统一文档中心
│   ├── user-guides/         # 用户指南
│   ├── developer-guides/    # 开发者指南
│   ├── reference/           # 参考文档
│   ├── project-management/  # 项目管理文档
│   │   ├── phases/          # 各阶段文档
│   │   ├── reports/         # 项目报告
│   │   └── phase-9-4-complete.md  # Phase 9.4完整报告
│   └── INDEX.md             # 文档索引
│
├── examples/                 # 使用示例
│   ├── basic.rs             # 基础使用示例
│   ├── advanced/            # 高级功能示例
│   └── benchmarks/          # 性能基准示例
│
├── exported_profiles/        # 导出的浏览器指纹配置 (66个)
│   ├── chrome_*.json        # Chrome系列配置
│   ├── firefox_*.json       # Firefox系列配置
│   └── ...                  # 其他浏览器配置
│
├── models/                   # 机器学习模型
│   └── feature_info.json    # 特征信息
│
├── output/                   # 输出文件
│   ├── data/                # 数据输出
│   │   ├── dns/            # DNS相关输出
│   │   ├── pcap/           # 数据包捕获
│   │   └── analysis/       # 分析结果
│   ├── logs/                # 日志文件
│   ├── reports/             # 生成报告
│   └── temp/                # 临时文件
│
├── scripts/                  # 脚本工具
│   ├── build/               # 构建脚本
│   ├── deploy/              # 部署脚本
│   ├── test/                # 测试脚本
│   └── maintenance/         # 维护脚本
│
├── tests/                    # 测试文件
│   ├── unit/                # 单元测试
│   ├── integration/         # 集成测试
│   ├── e2e/                 # 端到端测试
│   └── performance/         # 性能测试
│
├── .github/                  # GitHub配置
├── .cargo/                   # Cargo配置
└── README.md                 # 项目主文档
```

## 🎯 关键目录说明

### 核心代码目录
- **`crates/`**: Rust工作区，包含所有功能模块
- **`exported_profiles/`**: 66个预定义的浏览器指纹配置
- **`dataset/`**: 用于机器学习训练的数据集

### 配置管理
- **`config/`**: 所有配置文件的集中管理
- **`config/deployment/`**: 部署相关的K8s和Docker配置
- **`config/monitoring/`**: 监控系统的完整配置

### 文档体系
- **`docs/`**: 统一的文档中心，按用户角色和用途分类
- **`docs/project-management/`**: 项目历史文档和阶段报告

### 输出管理
- **`output/`**: 所有生成文件的集中存放地
- **`output/data/`**: 运行时产生的数据文件
- **`output/logs/`**: 系统和应用日志

## 📊 项目规模统计

- **Rust Crates**: 20个独立模块
- **浏览器指纹**: 66个预配置文件 (69+版本支持)
- **文档文件**: 80+个Markdown文件
- **测试用例**: 全面的单元和集成测试
- **示例代码**: 17个实用示例

## 🔧 开发环境

### 必需工具
- Rust 1.92.0+
- Cargo (Rust包管理器)
- Git

### 可选工具
- Docker (容器化部署)
- Kubernetes (集群部署)
- Prometheus/Grafana (监控)

### 常用命令
```bash
# 构建项目
cargo build --workspace --features "rustls-tls,http2,http3"

# 运行示例
cargo run --example basic

# 运行测试
cargo test

# 构建发布版本
cargo build --release
```

## 🚀 部署选项

### 本地开发
```bash
cargo run --example basic
```

### 生产部署
- **容器化**: 使用 `config/deployment/docker/` 配置
- **K8s部署**: 使用 `config/deployment/k8s/` 配置
- **传统部署**: 构建二进制文件直接运行

## 📈 性能指标

- **HTTP/3响应时间**: ~40.3ms
- **HTTP/2响应时间**: ~48.0ms  
- **HTTP/1.1响应时间**: ~44.4ms
- **测试通过率**: 100%
- **支持协议**: HTTP/1.1, HTTP/2, HTTP/3

---
*此文档反映项目当前的组织结构，会随项目发展而更新。*