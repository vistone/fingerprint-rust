# Google Earth API 全面测试指南

## 测试地址

**https://kh.google.com/rt/earth/PlanetoidMetadata**

这是一个真实的 Google Earth API 端点，支持 HTTP/1.1、HTTP/2 和 HTTP/3。

## 测试内容

测试所有 **69 个浏览器指纹**，每个指纹分别使用：
- HTTP/1.1
- HTTP/2
- HTTP/3

总计：**66 × 3 = 198 个测试用例**

## 运行测试

### 方法 1: 使用测试脚本（推荐）

```bash
bash scripts/test_google_earth_all_fingerprints.sh
```

### 方法 2: 使用 Cargo 命令

#### 测试 HTTP/1.1（所有 69 个指纹）

```bash
cargo test --test comprehensive_google_earth_test test_all_fingerprints_http1 \
    --features rustls-tls,http2,http3 \
    -- --ignored --nocapture
```

#### 测试 HTTP/2（所有 69 个指纹）

```bash
cargo test --test comprehensive_google_earth_test test_all_fingerprints_http2 \
    --features rustls-tls,http2,http3 \
    -- --ignored --nocapture
```

#### 测试 HTTP/3（所有 69 个指纹）

```bash
cargo test --test comprehensive_google_earth_test test_all_fingerprints_http3 \
    --features rustls-tls,http2,http3 \
    -- --ignored --nocapture
```

#### 全面测试（所有协议）

```bash
cargo test --test comprehensive_google_earth_test test_all_fingerprints_all_protocols \
    --features rustls-tls,http2,http3 \
    -- --ignored --nocapture
```

**注意**：全面测试会运行较长时间（约 198 个测试用例）

## 测试输出

测试会显示：
- 每个指纹的测试进度 `[1/66]`
- 测试结果（✅ 成功 或 ❌ 失败）
- 状态码和响应时间
- 最终统计汇总

### 示例输出

```
╔══════════════════════════════════════════════════════════╗
║  Google Earth API 全面测试 - HTTP/1.1                    ║
║  地址: https://kh.google.com/rt/earth/PlanetoidMetadata  ║
╚══════════════════════════════════════════════════════════╝

🔍 测试所有 69 个浏览器指纹 (HTTP/1.1)...

  [ 1/66] chrome_103                          ... ✅ 200 (450ms)
  [ 2/66] chrome_133                          ... ✅ 200 (452ms)
  [ 3/66] firefox_133                         ... ✅ 200 (451ms)
  ...

╔══════════════════════════════════════════════════════════╗
║  HTTP/1.1 测试结果汇总                        ║
╚══════════════════════════════════════════════════════════╝

  总测试数: 66
  成功: 66 ✅
  失败: 0 ❌
  成功率: 100.0%
  总耗时: 45.23s
```

## 测试验证

每个测试会验证：
1. ✅ **连接成功** - 能够建立 TLS 连接
2. ✅ **协议正确** - 返回的 HTTP 版本符合预期
3. ✅ **状态码 200** - 服务器返回成功响应
4. ✅ **响应非空** - 响应体包含数据

## 测试覆盖

### 浏览器类型

- **Chrome 系列**: 19 个版本
- **Firefox 系列**: 13 个版本
- **Safari 系列**: 9 个版本（包括 iOS）
- **Opera 系列**: 3 个版本
- **移动端和自定义**: 22 个指纹

### 协议支持

- ✅ HTTP/1.1 - 所有指纹
- ✅ HTTP/2 - 所有指纹（需要 `http2` feature）
- ✅ HTTP/3 - 所有指纹（需要 `http3` feature）

## 注意事项

1. **网络要求**: 需要能够访问 `kh.google.com`
2. **测试时间**: 全面测试需要较长时间（约 5-10 分钟）
3. **请求间隔**: 测试中已添加延迟，避免请求过快
4. **超时设置**: 每个请求超时时间为 10-15 秒

## 故障排查

### 如果测试失败

1. **检查网络连接**
   ```bash
   curl -I https://kh.google.com/rt/earth/PlanetoidMetadata
   ```

2. **检查 DNS 解析**
   ```bash
   nslookup kh.google.com
   ```

3. **查看详细错误**
   - 测试输出会显示每个失败的指纹和错误信息

### 常见错误

- **连接超时**: 网络问题或防火墙阻止
- **TLS 错误**: 证书验证问题（检查 `verify_tls` 配置）
- **协议不匹配**: 服务器不支持请求的 HTTP 版本

## 测试报告

测试完成后会生成统计报告，包括：
- 总测试数和成功率
- 按协议分类的统计
- 按浏览器类型分类的统计
- 失败的测试详情

## 相关文件

- 测试代码: `tests/comprehensive_google_earth_test.rs`
- 测试脚本: `scripts/test_google_earth_all_fingerprints.sh`
- 其他测试: `tests/kh_google_all_fingerprints_test.rs`（已有类似测试）

