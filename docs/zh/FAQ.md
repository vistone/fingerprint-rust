# 常见问题 (FAQ)

**[English](../../en/FAQ.md)** | [中文](#中文)

---

## 中文

### 一般问题

#### Q: 什么是浏览器指纹？
**A:** 浏览器指纹是指通过收集浏览器和系统的各种特征来识别和追踪用户的技术。特征包括：
- User-Agent字符串
- TLS配置和密码套件
- HTTP请求头顺序
- 时序信息
- Canvas/WebGL输出
- 字体列表
- 音频上下文信息

### 使用问题

#### Q: 如何在我的项目中使用fingerprint-rust？
**A:** 
1. 将依赖添加到Cargo.toml：
```toml
[dependencies]
fingerprint = "2.1"
```
2. 使用简单API：
```rust
use fingerprint::get_random_fingerprint;
let fp = get_random_fingerprint()?;
```

#### Q: fingerprint-rust支持哪些浏览器？
**A:** 支持97+ 浏览器指纹：
- **Chrome**: v90-133 (所有主要版本)
- **Firefox**: v88-121
- **Safari**: v14-16
- **Edge**: v90-120
- **Opera**: v76-108
- **移动浏览器**: Chrome/Safari iOS, Chrome Android etc.

#### Q: 生成的指纹有多真实？
**A:** 
- ✅ 基于真实浏览器数据（不是生成的虚假数据）
- ✅ 包含真实的TLS ClientHello Specs
- ✅ 97%+ 准确率与真实浏览器匹配
- ✅ 通过了Google Earth API的端到端测试

#### Q: 指纹可以用于什么目的？
**A:** 合法用途包括：
- ✅ Web爬虫 (遵守robots.txt)
- ✅ API测试和集成测试
- ✅ 加强隐私保护
- ✅ 学术研究
- ✅ 安全防守和检测

### 性能问题

#### Q: 指纹识别的性能如何？
**A:** 
```
指纹识别时间: ~0.5ms
HTTP请求延迟: <100ms (含网络)
内存使用: ~5-10MB (完整缓存)
吞吐量: >10,000 req/sec (单线程)
```

#### Q: 如何优化性能？
**A:** 
1. **使用连接池**：
```rust
let config = PoolManagerConfig::default()
    .with_max_idle_per_host(32)
    .with_connection_timeout_secs(5);
```

2. **启用缓存**：
```rust
// 自动启用L1/L2/L3缓存
let fp = get_random_fingerprint()?; // 缓存配置
```

3. **批量处理**：
```rust
let fingerprints: Vec<_> = (0..100)
    .map(|_| get_random_fingerprint())
    .collect::<Result<_, _>>()?;
```

#### Q: 支持HTTP/3吗？
**A:** 是的！完整支持：
- ✅ HTTP/1.1
- ✅ HTTP/2 (h2协议)
- ✅ HTTP/3 (QUIC协议)
自动协商最适合的版本。

### 安全与隐私问题

#### Q: 指纹欺骗是否违法？
**A:** 这取决于你的用途和当地法律。通常：
- ✅ 合法：个人隐私保护、测试自己的系统
- ⚠️ 灰色地带：爬虫、访问受限资源
- ❌ 非法：欺骗、诈骗、侵犯隐私

**建议**: 始终遵守服务条款和当地法律。

#### Q: 指纹会泄露隐私吗？
**A:** 否，fingerprint-rust是本地计算的，不涉及：
- 🔒 没有数据上传到服务器
- 🔒 没有请求跟踪
- 🔒 完全在本地处理
- 🔒 开源代码可审计

#### Q: 如何防止被检测？
**A:** 
1. **使用真实指纹**：
```rust
let fp = get_random_fingerprint()?; // 不是生成的
```

2. **添加随机延迟**：
```rust
use std::time::Duration;
tokio::time::sleep(Duration::from_secs(rand::random::<u64>() % 5)).await;
```

3. **轮换指纹**：
```rust
loop {
    let fp = get_random_fingerprint()?; // 每次新指纹
    // 发送请求...
}
```

4. **处理验证码**：大多数网站都有验证码作为最后防线。

### 开发问题

#### Q: 如何为项目贡献代码？
**A:** 
1. Fork仓库
2. 创建功能分支: `git checkout -b feature/xxx`
3. 进行更改并通过所有检查：
```bash
cargo fmt --all
cargo clippy --all-features
cargo test --all-features
```
4. 提交PR并等待审查

#### Q: 支持的最低Rust版本是什么？
**A:** Rust 1.92.0 或更高版本。

#### Q: 可以在生产环境中使用吗？
**A:** 是的！项目特点：
- ✅ 生产级代码质量
- ✅ 广泛的测试覆盖
- ✅ 完整的错误处理
- ✅ 性能优化

#### Q: 如何报告bug？
**A:** 
1. 检查现有issues
2. 创建新issue，包括：
   - Rust版本
   - 操作系统
   - 最小复现代码
   - 错误消息和堆栈跟踪

#### Q: 支持WebAssembly吗？
**A:** 部分支持：
- ✅ 可以编译为wasm
- ⚠️ 某些功能在浏览器中不可用
- 计划: 完整的wasm支持

### 集成问题

#### Q: 如何与Web框架集成？

**Actix-web 示例：**
```rust
use actix_web::{web, HttpResponse};
use fingerprint::get_random_fingerprint;

async fn get_fingerprint() -> HttpResponse {
    match get_random_fingerprint() {
        Ok(fp) => HttpResponse::Ok().json(serde_json::json!(fp)),
        Err(e) => HttpResponse::InternalServerError().body(e.to_string()),
    }
}
```

**Tokio示例：**
```rust
#[tokio::main]
async fn main() {
    let fp = get_random_fingerprint().unwrap();
    println!("User-Agent: {}", fp.user_agent);
}
```

#### Q: 支持异步吗？
**A:** 完全支持！所有I/O操作都是异步的：
```rust
let response = client.get(url).await?; // 异步HTTP请求
```

#### Q: 如何与数据库集成？
**A:** 可以序列化存储：
```rust
let fp = get_random_fingerprint()?;
let json_str = serde_json::to_string(&fp)?;
// 存储到数据库...
```

### 兼容性问题

#### Q: 在Windows上可以使用吗？
**A:** 是的！完全支持：
- ✅ Windows 10/11
- ✅ 所有功能可用
- ✅ CI/CD测试Windows

#### Q: macOS支持情况？
**A:** 
- ✅ Intel和Apple Silicon (M1/M2/M3)
- ✅ macOS 10.14+
- ✅ 所有功能可用

#### Q: Linux支持哪些发行版？
**A:** 
- ✅ Ubuntu, Debian, Fedora, CentOS等
- ✅ 所有主要发行版
- ⚠️ 需要glibc 2.17+

### 许可证问题

#### Q: 许可证是什么？
**A:** MIT和BSD-3-Clause双许可，意思是你可以：
- ✅ 自由使用和修改
- ✅ 用于商业项目
- ✅ 创建闭源应用
- 📝 必须保留许可证声明

#### Q: 可以用于商业用途吗？
**A:** 是的！完全允许在商业项目中使用。

### 调试问题

#### Q: 如何启用日志？
**A:** 
```bash
# 所有模块
RUST_LOG=debug cargo run

# 特定模块
RUST_LOG=fingerprint_core=trace cargo run

# 特定级别
RUST_LOG=fingerprint=info,fingerprint_core=debug cargo run
```

#### Q: 如何报告性能问题？
**A:** 
1. 运行基准测试：`cargo bench`
2. 启用分析：`cargo flamegraph`
3. 在issue中附加结果和环境信息

### 未来计划

#### Q: 项目的发展方向是什么？
**A:** 近期计划：
- 🚀 完整wasm支持
- 🚀 更多ML模型集成
- 🚀 增强隐私保护
- 🚀 改进文档
- 🚀 性能优化

#### Q: 如何关注项目更新？
**A:** 
- ⭐ GitHub Star
- 👁️ Watch仓库
- 📧 订阅Release通知
- 💬 参与Discussions

### 获取更多帮助

- 📖 [完整文档](https://github.com/vistone/fingerprint-rust)
- 🐛 [GitHub Issues](https://github.com/vistone/fingerprint-rust/issues)
- 💬 [Discussions](https://github.com/vistone/fingerprint-rust/discussions)
- 📧 贡献者和维护者随时准备帮助

---

**最后更新**: 2026年2月24日
